server {
    listen 80;

    # Serve React app
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    # Reverse proxy to Spring
    location /api/ {
        proxy_pass http://api:8080/;
    }
}
